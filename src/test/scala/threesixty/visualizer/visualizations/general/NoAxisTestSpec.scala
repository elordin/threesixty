package threesixty.visualizer.visualizations.general

import org.scalatest.FunSpec

/*
class NoAxisTestSpec extends FunSpec {

    describe("A NoAxis") {
        val axis = AxisFactory.createAxis(AxisType.Nothing, AxisDimension.xAxis, 100, -1, -1, "label", Some(5), Some("10"))

        it("should calculate the correct number of grid points") {
            assert(axis.getNumberOfGridPoints == 0)
        }

        it("should calculate the correct unit") {
            assert(axis.getUnit == 0)
        }

        it("should get the correct minimum and maximum value displayed value") {
            assert(axis.getMinimumDisplayedValue == 0)
            assert(axis.getMaximumDisplayedValue == 100)
        }

        it("should correctly convert a value") {
            assert(axis.convert(30) == 30)
        }

        it("should get the correct grid labels") {
            assert(axis.getGridLabels.isEmpty)
        }

        it("should get the correct grid points and label") {
            assert(axis.getGridPointsAndLabel.isEmpty)
        }

        it("should get the correct axis label") {
            assertResult("label") {
                axis.getAxisLabel
            }
        }
    }
}
*/
